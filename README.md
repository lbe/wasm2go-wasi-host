# wasm2go-wasi-host

[![Go Reference](https://pkg.go.dev/badge/github.com/lbe/wasm2go-wasi-host.svg)](https://pkg.go.dev/github.com/lbe/wasm2go-wasi-host)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)
[![Go Version](https://img.shields.io/badge/Go-1.26.3-blue.svg)](https://go.dev/dl/)
[![Go Report Card](https://goreportcard.com/badge/github.com/lbe/wasm2go-wasi-host)](https://goreportcard.com/report/github.com/lbe/wasm2go-wasi-host)
[![CI](https://github.com/lbe/wasm2go-wasi-host/actions/workflows/ci.yml/badge.svg)](https://github.com/lbe/wasm2go-wasi-host/actions/workflows/ci.yml)
[![Release](https://img.shields.io/github/v/release/lbe/wasm2go-wasi-host)](https://github.com/lbe/wasm2go-wasi-host/releases)

A standalone, general-purpose **WASI snapshot-preview1** host for Go, specifically designed for modules transpiled by [wasm2go](https://github.com/mpx/wasm2go).

It implements all 40 WASI preview1 functions plus the `env.call_host_function` stub required by zeroperl-style modules. Unlike `wazero` or other heavy runtimes, this host is designed to work directly with the generated code from `wasm2go`, providing a lightweight, standard-library-only integration for running WebAssembly binaries as native Go.

## Features

- **Full WASI Preview1 Support**: Implementation of all 40 system calls.
- **wasm2go-Native**: Direct memory access via guest memory callbacks—no overhead from external runtime APIs.
- **Capability-oriented Virtual Filesystem**: Support for multiple mounts, including read-only `fs.FS` (embedded files) and writable host directory mounts.
- **Standard Library Only**: No external dependencies.
- **CLI Runner**: Includes `wasm2go-run`, a tool that transpiles, compiles, and executes WASM files in one step.

## Installation

```bash
go get github.com/lbe/wasm2go-wasi-host
```

## Usage

### Using as a Library

To use the host with a `wasm2go`-generated module:

```go
import (
    "os"
    "github.com/lbe/wasm2go-wasi-host"
    "github.com/your/generated/package" // module generated by wasm2go
)

func main() {
    // 1. Initialize the WASI host state.
    // The memory callback is re-invoked on every syscall to handle memory growth.
    var mod *generated.Module
    state := wasihost.New(
        func() []byte { return *mod.Xmemory().Slice() },
        wasihost.WithArgs("app-name", "arg1"),
        wasihost.WithEnv("PATH=/bin"),
        wasihost.WithWritableMount("/", "./host-data", os.DirFS("./host-data")),
        wasihost.WithStdout(os.Stdout),
        wasihost.WithStderr(os.Stderr),
    )

    // 2. Instantiate the generated module with the host state as the import resolver.
    mod = generated.New(state, state)

    // 3. Run the guest's start function (usually _start).
    defer func() {
        if r := recover(); r != nil {
            if e, ok := r.(wasihost.ExitError); ok {
                os.Exit(int(e.Code))
            }
            panic(r)
        }
    }()
    mod.X_start()
}
```

### Using the CLI Runner

The `wasm2go-run` tool automates the process of transpiling a WASM file to Go, compiling it, and running it with the WASI host.

```bash
go install github.com/lbe/wasm2go-wasi-host/cmd/wasm2go-run@latest

# Run a WASM binary with mounts and environment variables
wasm2go-run -dir ./data:/data -env KEY=VALUE my-app.wasm -- --app-arg1
```

## File System Support

`wasm2go-wasi-host` provides a flexible mounting system:

- **`WithMount(guestPath, root)`**: Mounts a read-only `fs.FS` at the specified guest path.
- **`WithWritableMount(guestPath, hostRoot, root)`**: Mounts a writable host directory. It uses `root` (optional `fs.FS`) for read-through lookups and `hostRoot` for all write operations.

## Concurrency and Safety

- **Single-owner**: The `State` is not safe for concurrent use. Each `wasm2go` Module instance must have its own `State`, and both must be used from a single goroutine.
- **Goroutine Assertion**: You can enable `WithOwnerAssertion()` to panic if the host is accessed from multiple goroutines, helping catch concurrency bugs early.

## License

MIT
